import Layout from '@/layouts/Main';
import Section from '@/components/Section';

export default () => (
  <Layout>
    <Section fullscreen={true}>
      <h1>About page</h1>
    </Section>
  </Layout>
);
