import Layout from '@/components/Layout';
import Section from '@/components/Section';

export default () => (
  <Layout>
    <Section fullscreen={true}>
      <h1>About page</h1>
    </Section>
  </Layout>
);
