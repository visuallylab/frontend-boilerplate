import Layout from '../components/Layout';
import Section from '../components/Section';

export default () => (
  <Layout>
    <Section fullscreen>
      <h1>About page</h1>
    </Section>
  </Layout>
);
